{
  "name": "gitflare",
  "version": "0.0.0",
  "type": "module",
  "description": "CLI for GitFlare — install, admin, escape hatches",
  "license": "SEE LICENSE IN ../../LICENSE",
  "bin": {
    "gitflare": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "@gitflare/shared": "workspace:*",
    "commander": "^12.1.0",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
