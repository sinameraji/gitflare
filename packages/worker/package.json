{
  "name": "@gitflare/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@gitflare/shared": "workspace:*",
    "hono": "^4.6.0",
    "isomorphic-git": "^1.27.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.80.0"
  }
}
